name: 'Install GitHub CLI'
description: 'Download, cache and install the GH CLI to the workspace'
author: 'arimal199-org'
inputs:
  gh-cli-version:
    description: 'Version of the GitHub-CLI that should be installed'
    required: false
    default: '2.44.1'
runs:
  using: 'node21'
  main: 'dist/index.js'
